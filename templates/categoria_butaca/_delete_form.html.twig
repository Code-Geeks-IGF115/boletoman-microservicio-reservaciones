<form method="post" action="{{ path('app_categoria_butaca_delete', {'id': categoria_butaca.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categoria_butaca.id) }}">
    <button class="btn">Delete</button>
</form>
