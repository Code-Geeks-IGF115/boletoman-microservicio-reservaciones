{% extends 'base.html.twig' %}

{% block title %}Disponibilidad{% endblock %}

{% block body %}
    <h1>Disponibilidad</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ disponibilidad.id }}</td>
            </tr>
            <tr>
                <th>Disponible</th>
                <td>{{ disponibilidad.disponible }}</td>
            </tr>
            <tr>
                <th>IdEvento</th>
                <td>{{ disponibilidad.idEvento }}</td>
            </tr>
            <tr>
                <th>IdDetalleCompra</th>
                <td>{{ disponibilidad.idDetalleCompra }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_disponibilidad_index') }}">back to list</a>

    <a href="{{ path('app_disponibilidad_edit', {'id': disponibilidad.id}) }}">edit</a>

    {{ include('disponibilidad/_delete_form.html.twig') }}
{% endblock %}
