{% extends 'base.html.twig' %}

{% block title %}Disponibilidad index{% endblock %}

{% block body %}
    <h1>Disponibilidad index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Disponible</th>
                <th>IdEvento</th>
                <th>IdDetalleCompra</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for disponibilidad in disponibilidads %}
            <tr>
                <td>{{ disponibilidad.id }}</td>
                <td>{{ disponibilidad.disponible }}</td>
                <td>{{ disponibilidad.idEvento }}</td>
                <td>{{ disponibilidad.idDetalleCompra }}</td>
                <td>
                    <a href="{{ path('app_disponibilidad_show', {'id': disponibilidad.id}) }}">show</a>
                    <a href="{{ path('app_disponibilidad_edit', {'id': disponibilidad.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_disponibilidad_new') }}">Create new</a>
{% endblock %}
