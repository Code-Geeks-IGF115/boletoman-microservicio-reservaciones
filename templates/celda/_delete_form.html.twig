<form method="post" action="{{ path('app_celda_delete', {'id': celda.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ celda.id) }}">
    <button class="btn">Delete</button>
</form>
